/**
 * FgEnvironmentStorageConfigInterface -
 * Interface describes the available properties that can be configured
 * for overrides in http interceptors
 */
export interface FgEnvironmentStorageConfigInterface {}
